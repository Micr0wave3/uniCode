#include <stdio.h>

int main()
{
    char str[100];
    printf("You have 100 characters to insert and store: \n");
    scanf("%s", &str);
    printf("%s", str);
    getch ();
    return 0;
}